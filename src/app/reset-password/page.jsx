import { ResetPassword } from "../../components/auth/PasswordRecovery";

export const metadata = { title: "Reset password · MiLink", robots: { index: false, follow: false } };

export default function ResetPasswordPage() { return <ResetPassword/>; }
